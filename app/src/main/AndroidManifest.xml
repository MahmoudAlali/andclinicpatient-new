<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dcoret.beautyclient">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EditReservation"></activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".GoogleMapBeauty"
            android:label="@string/title_activity_google_map" />
        <activity android:name=".Activities.AddAndSearchPlace" />
        <activity android:name=".AddLocation" />
        <activity android:name=".ReservationTime" />
        <activity android:name=".ResevationDate" />
        <activity android:name=".AddReservation" />
        <activity android:name=".Activities.Payment" />
        <activity android:name=".Activities.BrideServicesSelecting" />

        <service android:name=".Service.MyFirebaseInstanceService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <receiver
            android:name=".Activities.SMSReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.auth.api.phone.SMS_RETRIEVED" />
            </intent-filter>
        </receiver>

        <activity android:name=".Activities.Setting" />
        <activity android:name=".Activities.Login" />
        <activity android:name=".Activities.More" />
        <activity android:name=".Activities.MapFiltering" />
        <activity android:name=".Activities.BeautyMainPage_2" />
        <activity android:name=".Activities.SelectServiceForOffer" />

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <activity android:name=".Activities.Compartion" />
        <activity android:name=".Activities.Help" />
        <activity android:name=".Activities.Favorites" />
        <activity android:name=".Activities.Search" />
        <activity android:name=".Activities.ShoppingCart" />
        <activity android:name=".Activities.SearchInterface" />
        <activity android:name=".Activities.Notification" />
        <activity android:name=".Activities.ReservationDetails" />
        <activity android:name=".Activities.OfferDetails" />
        <activity android:name=".Activities.ServiceDetails" />
        <activity android:name=".Activities.EditProfile" />
        <activity
            android:name=".Activities.ManageAccounts"
            android:label="@string/title_activity_manage_accounts"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activities.CreateOffers" />
        <activity android:name=".Activities.ServiceType" />
        <activity android:name=".Activities.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.Login1" />
        <activity android:name=".Activities.Register" />
        <activity android:name=".Activities.MainPage" />
        <activity android:name=".Activities.ForgetMyPass" />
        <activity android:name=".Activities.Services" />
        <activity android:name=".Activities.Offers" />
        <activity android:name=".Activities.Reservation" />
        <activity android:name=".Activities.ManageAccount" />
        <activity android:name=".Activities.ExtraServices" />
        <activity android:name=".Activities.CustomerService" />
        <activity android:name=".Activities.FavoritesList" />
        <activity android:name=".Activities.MyProfile" />
        <activity android:name=".Activities.Point" />
        <activity android:name=".Activities.BeautyMainPage" />
        <activity
            android:name=".Activities.MyReservations"
            android:label="@string/title_activity_my_reservations"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Main2Activity"
            android:label="@string/title_activity_main2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MyResrvation"
            android:label="@string/title_activity_my_resrvation"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MyReservation"
            android:label="@string/title_activity_my_reservation"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>